<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Admin Panel</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Roboto, sans-serif;
            background-color: #f9f9f9;
            display: flex;
            min-height: 100vh;
        }
        
        /* Sidebar */
        .sidebar {
            width: 250px;
            background: linear-gradient(135deg, #d81b60 0%, #c2185b 100%);
            color: white;
            padding: 20px 0;
            flex-shrink: 0;
            box-shadow: 2px 0 10px rgba(0,0,0,0.1);
            position: fixed;
            height: 100vh;
            overflow-y: auto;
        }
        
        .sidebar-header {
            padding: 0 20px 20px 20px;
            border-bottom: 1px solid rgba(255,255,255,0.2);
            margin-bottom: 20px;
        }
        
        .sidebar-header h3 {
            font-size: 1.5rem;
            font-weight: 600;
            color: white;
            text-align: center;
        }
        
        .sidebar-menu {
            list-style: none;
        }
        
        .sidebar-menu li {
            margin-bottom: 5px;
        }
        
        .sidebar-menu a {
            color: white;
            text-decoration: none;
            display: block;
            padding: 12px 20px;
            transition: all 0.3s ease;
            border-radius: 0 25px 25px 0;
            margin-right: 10px;
            font-weight: 500;
        }
        
        .sidebar-menu a:hover {
            background-color: rgba(255,255,255,0.2);
            transform: translateX(5px);
        }
        
        .sidebar-menu a.active {
            background-color: rgba(255,255,255,0.3);
            border-left: 4px solid white;
        }
        
        /* Main Content */
        .main-content {
            flex-grow: 1;
            margin-left: 250px;
            padding: 30px;
            background-color: #f9f9f9;
            min-height: 100vh;
        }
        
        .page-header {
            background: white;
            padding: 25px 30px;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            margin-bottom: 30px;
        }
        
        .page-header h1 {
            color: #d81b60;
            font-size: 2rem;
            font-weight: 600;
            margin: 0;
        }
        
        .page-header p {
            color: #666;
            margin-top: 8px;
            font-size: 1rem;
        }
        
        /* Stats Cards */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            text-align: center;
            transition: transform 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
        }
        
        .stat-card .icon {
            color: #d81b60;
            font-size: 3rem;
            margin-bottom: 15px;
            display: block;
        }
        
        .stat-card h3 {
            color: #666;
            font-size: 1rem;
            margin-bottom: 10px;
            font-weight: 500;
        }
        
        .stat-card .number {
            color: #d81b60;
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .stat-card .label {
            color: #999;
            font-size: 0.9rem;
        }
        
        /* Quick Actions */
        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .action-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .action-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        
        .action-card .icon {
            font-size: 3rem;
            margin-bottom: 15px;
            display: block;
        }
        
        .action-card h3 {
            color: #333;
            font-size: 1.2rem;
            margin-bottom: 10px;
        }
        
        .action-card p {
            color: #666;
            font-size: 0.9rem;
            line-height: 1.5;
        }
        
        /* Recent Activities */
        .recent-activities {
            background: white;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            overflow: hidden;
        }
        
        .activities-header {
            background: linear-gradient(135deg, #d81b60 0%, #c2185b 100%);
            color: white;
            padding: 20px 30px;
        }
        
        .activities-header h2 {
            margin: 0;
            font-size: 1.3rem;
            font-weight: 500;
        }
        
        .activity-list {
            list-style: none;
            padding: 0;
        }
        
        .activity-item {
            display: flex;
            align-items: center;
            padding: 20px 30px;
            border-bottom: 1px solid #e9ecef;
            transition: background-color 0.3s ease;
        }
        
        .activity-item:hover {
            background-color: #f8f9fa;
        }
        
        .activity-item:last-child {
            border-bottom: none;
        }
        
        .activity-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            font-size: 1.2rem;
        }
        
        .activity-icon.posts {
            background: linear-gradient(135deg, #d81b60 0%, #c2185b 100%);
            color: white;
        }
        
        .activity-icon.products {
            background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
            color: white;
        }
        
        .activity-icon.users {
            background: linear-gradient(135deg, #4caf50 0%, #388e3c 100%);
            color: white;
        }
        
        .activity-content {
            flex: 1;
        }
        
        .activity-title {
            font-weight: 600;
            color: #333;
            margin-bottom: 5px;
        }
        
        .activity-time {
            color: #666;
            font-size: 0.9rem;
        }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .sidebar {
                width: 200px;
            }
            .main-content {
                margin-left: 200px;
            }
        }
        
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
                z-index: 1000;
            }
            .sidebar.open {
                transform: translateX(0);
            }
            .main-content {
                margin-left: 0;
                padding: 20px;
            }
            .stats-container {
                grid-template-columns: 1fr;
            }
            .quick-actions {
                grid-template-columns: 1fr;
            }
        }
        
        /* Mobile menu toggle */
        .mobile-menu-toggle {
            display: none;
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 1001;
            background: #d81b60;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
        }
        
        @media (max-width: 768px) {
            .mobile-menu-toggle {
                display: block;
            }
        }
    </style>
</head>
<body>
    <!-- Mobile menu toggle -->
    <button class="mobile-menu-toggle" onclick="toggleSidebar()">☰</button>
    
    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <h3>🎯 Admin Panel</h3>
        </div>
        <ul class="sidebar-menu">
            <li><a href="/admin" class="active">
                📊 Dashboard
            </a></li>
            <li><a href="/admin/posts">
                📝 Bài viết
            </a></li>
            <li><a href="/admin/products">
                📦 Sản phẩm
            </a></li>
            <li><a href="/admin/users">
                👥 Người dùng
            </a></li>
            <li><a href="/admin/settings">
                ⚙️ Cài đặt
            </a></li>
            <li><a href="/">
                🏠 Về trang chủ
            </a></li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="page-header">
            <h1>📊 Dashboard</h1>
            <p>Chào mừng bạn đến với Admin Panel - Quản lý tổng quan hệ thống</p>
        </div>

        <!-- Stats Cards -->
        <div class="stats-container">
            <div class="stat-card">
                <div class="icon">📝</div>
                <h3>Tổng số bài viết</h3>
                <div class="number"><%= stats.totalPosts %></div>
                <div class="label">bài viết</div>
            </div>
            <div class="stat-card">
                <div class="icon">📦</div>
                <h3>Tổng số sản phẩm</h3>
                <div class="number"><%= stats.totalProducts %></div>
                <div class="label">sản phẩm</div>
            </div>
            <div class="stat-card">
                <div class="icon">👥</div>
                <h3>Tổng số người dùng</h3>
                <div class="number"><%= stats.totalUsers %></div>
                <div class="label">người dùng</div>
            </div>
            <div class="stat-card">
                <div class="icon">💰</div>
                <h3>Doanh thu tháng</h3>
                <div class="number"><%= (stats.totalProducts * 100000).toLocaleString('vi-VN') %></div>
                <div class="label">VNĐ</div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="quick-actions">
            <a href="/admin/products/add" class="action-card">
                <div class="icon">📦</div>
                <h3>Thêm Sản Phẩm</h3>
                <p>Thêm sản phẩm mới vào hệ thống</p>
            </a>
            <a href="/admin/posts/add" class="action-card">
                <div class="icon">📝</div>
                <h3>Viết Bài</h3>
                <p>Tạo bài viết mới cho website</p>
            </a>
            <a href="/admin/users" class="action-card">
                <div class="icon">👥</div>
                <h3>Quản Lý User</h3>
                <p>Xem và quản lý người dùng</p>
            </a>
            <a href="/admin/categories" class="action-card">
                <div class="icon">🗂️</div>
                <h3>Danh Mục</h3>
                <p>Quản lý danh mục sản phẩm</p>
            </a>
        </div>

        <!-- Recent Activities -->
        <div class="recent-activities">
            <div class="activities-header">
                <h2>🕒 Hoạt Động Gần Đây</h2>
            </div>
            <ul class="activity-list">
                <% if (typeof recentProducts !== 'undefined' && recentProducts.length > 0) { %>
                    <% recentProducts.forEach(product => { %>
                        <li class="activity-item">
                            <div class="activity-icon products">📦</div>
                            <div class="activity-content">
                                <div class="activity-title">Sản phẩm mới: <%= product.name %></div>
                                <div class="activity-time"><%= new Date(product.createdAt).toLocaleDateString('vi-VN') %></div>
                            </div>
                        </li>
                    <% }); %>
                <% } %>
                
                <% if (typeof recentPosts !== 'undefined' && recentPosts.length > 0) { %>
                    <% recentPosts.forEach(post => { %>
                        <li class="activity-item">
                            <div class="activity-icon posts">📝</div>
                            <div class="activity-content">
                                <div class="activity-title">Bài viết mới: <%= post.title %></div>
                                <div class="activity-time"><%= new Date(post.createdAt).toLocaleDateString('vi-VN') %></div>
                            </div>
                        </li>
                    <% }); %>
                <% } %>
                
                <% if ((typeof recentProducts === 'undefined' || recentProducts.length === 0) && 
                       (typeof recentPosts === 'undefined' || recentPosts.length === 0)) { %>
                    <li class="activity-item">
                        <div class="activity-icon posts">📝</div>
                        <div class="activity-content">
                            <div class="activity-title">Chưa có hoạt động nào</div>
                            <div class="activity-time">Hãy bắt đầu sử dụng hệ thống!</div>
                        </div>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('open');
        }
        
        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const mobileToggle = document.querySelector('.mobile-menu-toggle');
            
            if (window.innerWidth <= 768) {
                if (!sidebar.contains(event.target) && !mobileToggle.contains(event.target)) {
                    sidebar.classList.remove('open');
                }
            }
        });
        
        // Auto-refresh dashboard every 30 seconds to show new data
        setInterval(() => {
            location.reload();
        }, 30000);
    </script>
</body>
</html>
