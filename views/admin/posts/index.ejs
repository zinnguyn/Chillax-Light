<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Bài Viết | Admin Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Roboto, sans-serif;
            background-color: #f9f9f9;
            display: flex;
            min-height: 100vh;
        }
        
        /* Sidebar */
        .sidebar {
            width: 250px;
            background: linear-gradient(135deg, #d81b60 0%, #c2185b 100%);
            color: white;
            padding: 20px 0;
            flex-shrink: 0;
            box-shadow: 2px 0 10px rgba(0,0,0,0.1);
            position: fixed;
            height: 100vh;
            overflow-y: auto;
        }
        
        .sidebar-header {
            padding: 0 20px 20px 20px;
            border-bottom: 1px solid rgba(255,255,255,0.2);
            margin-bottom: 20px;
        }
        
        .sidebar-header h3 {
            font-size: 1.5rem;
            font-weight: 600;
            color: white;
            text-align: center;
        }
        
        .sidebar-menu {
            list-style: none;
        }
        
        .sidebar-menu li {
            margin-bottom: 5px;
        }
        
        .sidebar-menu a {
            color: white;
            text-decoration: none;
            display: block;
            padding: 12px 20px;
            transition: all 0.3s ease;
            border-radius: 0 25px 25px 0;
            margin-right: 10px;
            font-weight: 500;
        }
        
        .sidebar-menu a:hover {
            background-color: rgba(255,255,255,0.2);
            transform: translateX(5px);
        }
        
        .sidebar-menu a.active {
            background-color: rgba(255,255,255,0.3);
            border-left: 4px solid white;
        }
        
        /* Main Content */
        .main-content {
            flex-grow: 1;
            margin-left: 250px;
            padding: 30px;
            background-color: #f9f9f9;
            min-height: 100vh;
        }
        
        .page-header {
            background: white;
            padding: 25px 30px;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .page-header h1 {
            color: #d81b60;
            font-size: 2rem;
            font-weight: 600;
            margin: 0;
        }
        
        .btn-add {
            background: linear-gradient(135deg, #d81b60 0%, #c2185b 100%);
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        
        .btn-add:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(216, 27, 96, 0.3);
        }
        
        /* Stats Cards */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            text-align: center;
            transition: transform 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
        }
        
        .stat-card h3 {
            color: #666;
            font-size: 1rem;
            margin-bottom: 10px;
            font-weight: 500;
        }
        
        .stat-card .number {
            color: #d81b60;
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .stat-card .label {
            color: #999;
            font-size: 0.9rem;
        }
        
        /* Tabs */
        .tabs-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            overflow: hidden;
            margin-bottom: 30px;
        }
        
        .tabs-header {
            display: flex;
            background: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
        }
        
        .tab-button {
            flex: 1;
            padding: 20px;
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 500;
            color: #666;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .tab-button:hover {
            background-color: #f1f3f4;
            color: #d81b60;
        }
        
        .tab-button.active {
            background-color: white;
            color: #d81b60;
            border-bottom: 3px solid #d81b60;
        }
        
        .tab-button .count {
            background: #d81b60;
            color: white;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.8rem;
            margin-left: 8px;
        }
        
        .tab-content {
            padding: 30px;
            display: none;
        }
        
        .tab-content.active {
            display: block;
        }
        
        /* Table */
        .table-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            overflow: hidden;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        table th, table td {
            padding: 15px 20px;
            text-align: left;
            border-bottom: 1px solid #e9ecef;
        }
        
        table th {
            background-color: #f8f9fa;
            font-weight: 600;
            color: #333;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        table tr:hover {
            background-color: #f8f9fa;
        }
        
        .status-badge {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
            text-transform: uppercase;
        }
        
        .status-draft {
            background-color: #fff3cd;
            color: #856404;
        }
        
        .status-published {
            background-color: #d4edda;
            color: #155724;
        }
        
        .status-archived {
            background-color: #f8d7da;
            color: #721c24;
        }
        
        .action-buttons {
            display: flex;
            gap: 8px;
        }
        
        .btn-edit {
            background: linear-gradient(135deg, #ffc107 0%, #ff9800 100%);
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 6px;
            font-size: 0.85rem;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .btn-edit:hover {
            transform: translateY(-1px);
            box-shadow: 0 2px 8px rgba(255, 193, 7, 0.3);
        }
        
        .btn-delete {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 6px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .btn-delete:hover {
            transform: translateY(-1px);
            box-shadow: 0 2px 8px rgba(220, 53, 69, 0.3);
        }
        
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #666;
        }
        
        .empty-state h3 {
            margin-bottom: 10px;
            color: #999;
        }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .sidebar {
                width: 200px;
            }
            .main-content {
                margin-left: 200px;
            }
        }
        
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
                z-index: 1000;
            }
            .sidebar.open {
                transform: translateX(0);
            }
            .main-content {
                margin-left: 0;
                padding: 20px;
            }
            .stats-container {
                grid-template-columns: 1fr;
            }
            .tabs-header {
                flex-direction: column;
            }
            .tab-button {
                text-align: center;
            }
            .page-header {
                flex-direction: column;
                gap: 20px;
                text-align: center;
            }
        }
        
        /* Mobile menu toggle */
        .mobile-menu-toggle {
            display: none;
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 1001;
            background: #d81b60;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
        }
        
        @media (max-width: 768px) {
            .mobile-menu-toggle {
                display: block;
            }
        }
    </style>
</head>
<body>
    <!-- Mobile menu toggle -->
    <button class="mobile-menu-toggle" onclick="toggleSidebar()">☰</button>
    
    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <h3>🎯 Admin Panel</h3>
        </div>
        <ul class="sidebar-menu">
            <li><a href="/admin" class="<%= typeof activePage !== 'undefined' && activePage === 'dashboard' ? 'active' : '' %>">
                📊 Dashboard
            </a></li>
            <li><a href="/admin/posts" class="<%= typeof activePage !== 'undefined' && activePage === 'posts' ? 'active' : '' %>">
                📝 Bài viết
            </a></li>
            <li><a href="/admin/products" class="<%= typeof activePage !== 'undefined' && activePage === 'products' ? 'active' : '' %>">
                📦 Sản phẩm
            </a></li>

            <li><a href="/admin/users" class="<%= typeof activePage !== 'undefined' && activePage === 'users' ? 'active' : '' %>">
                👥 Người dùng
            </a></li>
            <li><a href="/admin/settings" class="<%= typeof activePage !== 'undefined' && activePage === 'settings' ? 'active' : '' %>">
                ⚙️ Cài đặt
            </a></li>
            <li><a href="/" class="<%= typeof activePage !== 'undefined' && activePage === 'home' ? 'active' : '' %>">
                🏠 Về trang chủ
            </a></li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="page-header">
            <h1>📝 Quản Lý Bài Viết</h1>
            <a href="/admin/posts/add" class="btn-add">✨ Thêm Bài Viết Mới</a>
        </div>

        <!-- Stats Cards -->
        <div class="stats-container">
            <div class="stat-card">
                <h3>Tổng số bài viết</h3>
                <div class="number"><%= posts ? posts.length : 0 %></div>
                <div class="label">bài viết</div>
            </div>
            <div class="stat-card">
                <h3>Bài viết đã xuất bản</h3>
                <div class="number"><%= posts ? posts.filter(p => p.status === 'published').length : 0 %></div>
                <div class="label">đã xuất bản</div>
            </div>
            <div class="stat-card">
                <h3>Bài viết nháp</h3>
                <div class="number"><%= posts ? posts.filter(p => p.status === 'draft').length : 0 %></div>
                <div class="label">bản nháp</div>
            </div>
            <div class="stat-card">
                <h3>Bài viết lưu trữ</h3>
                <div class="number"><%= posts ? posts.filter(p => p.status === 'archived').length : 0 %></div>
                <div class="label">đã lưu trữ</div>
            </div>
        </div>

        <!-- Tabs -->
        <div class="tabs-container">
            <div class="tabs-header">
                <button class="tab-button active" onclick="showTab('all')">
                    Tất cả bài viết
                    <span class="count"><%= posts ? posts.length : 0 %></span>
                </button>
                <button class="tab-button" onclick="showTab('published')">
                    Đã xuất bản
                    <span class="count"><%= posts ? posts.filter(p => p.status === 'published').length : 0 %></span>
                </button>
                <button class="tab-button" onclick="showTab('draft')">
                    Bản nháp
                    <span class="count"><%= posts ? posts.filter(p => p.status === 'draft').length : 0 %></span>
                </button>
                <button class="tab-button" onclick="showTab('archived')">
                    Lưu trữ
                    <span class="count"><%= posts ? posts.filter(p => p.status === 'archived').length : 0 %></span>
                </button>
            </div>

            <!-- Tab Content - All Posts -->
            <div id="tab-all" class="tab-content active">
                <div class="table-container">
<% if (posts && posts.length > 0) { %>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Tiêu đề</th>
                <th>Danh mục</th>
                                    <th>Loại</th>
                <th>Trạng thái</th>
                <th>Ngày tạo</th>
                <th>Hành động</th>
            </tr>
        </thead>
        <tbody>
            <% posts.forEach(post => { %>
                <tr>
                    <td><%= post.id %></td>
                                        <td><strong><%= post.title %></strong></td>
                                        <td><%= post.category ? post.category.name : 'N/A' %></td>
                                        <td><%= post.type || 'N/A' %></td>
                                        <td>
                                            <span class="status-badge status-<%= post.status %>">
                                                <%= post.status === 'published' ? 'Đã xuất bản' : 
                                                   post.status === 'draft' ? 'Bản nháp' : 
                                                   post.status === 'archived' ? 'Lưu trữ' : post.status %>
                                            </span>
                                        </td>
                                        <td><%= post.createdAt ? new Date(post.createdAt).toLocaleDateString('vi-VN') : 'N/A' %></td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/admin/posts/edit/<%= post.id %>" class="btn-edit">✏️ Sửa</a>
                                                <form action="/admin/posts/delete/<%= post.id %>" method="POST" style="display: inline-block;">
                                                    <button type="submit" class="btn-delete" onclick="return confirm('Bạn có chắc chắn muốn xóa bài viết này không?');">🗑️ Xóa</button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="empty-state">
                            <h3>📝 Chưa có bài viết nào</h3>
                            <p>Hãy tạo bài viết đầu tiên để bắt đầu!</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Tab Content - Published Posts -->
            <div id="tab-published" class="tab-content">
                <div class="table-container">
                    <% 
                    const publishedPosts = posts ? posts.filter(p => p.status === 'published') : [];
                    %>
                    <% if (publishedPosts.length > 0) { %>
                        <table>
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Tiêu đề</th>
                                    <th>Danh mục</th>
                                    <th>Loại</th>
                                    <th>Ngày xuất bản</th>
                                    <th>Hành động</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% publishedPosts.forEach(post => { %>
                                    <tr>
                                        <td><%= post.id %></td>
                                        <td><strong><%= post.title %></strong></td>
                                        <td><%= post.category ? post.category.name : 'N/A' %></td>
                                        <td><%= post.type || 'N/A' %></td>
                                        <td><%= post.updatedAt ? new Date(post.updatedAt).toLocaleDateString('vi-VN') : 'N/A' %></td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/admin/posts/edit/<%= post.id %>" class="btn-edit">✏️ Sửa</a>
                                                <form action="/admin/posts/delete/<%= post.id %>" method="POST" style="display: inline-block;">
                                                    <button type="submit" class="btn-delete" onclick="return confirm('Bạn có chắc chắn muốn xóa bài viết này không?');">🗑️ Xóa</button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="empty-state">
                            <h3>✅ Chưa có bài viết nào được xuất bản</h3>
                            <p>Hãy xuất bản bài viết đầu tiên!</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Tab Content - Draft Posts -->
            <div id="tab-draft" class="tab-content">
                <div class="table-container">
                    <% 
                    const draftPosts = posts ? posts.filter(p => p.status === 'draft') : [];
                    %>
                    <% if (draftPosts.length > 0) { %>
                        <table>
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Tiêu đề</th>
                                    <th>Danh mục</th>
                                    <th>Loại</th>
                                    <th>Ngày tạo</th>
                                    <th>Hành động</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% draftPosts.forEach(post => { %>
                                    <tr>
                                        <td><%= post.id %></td>
                                        <td><strong><%= post.title %></strong></td>
                                        <td><%= post.category ? post.category.name : 'N/A' %></td>
                                        <td><%= post.type || 'N/A' %></td>
                                        <td><%= post.createdAt ? new Date(post.createdAt).toLocaleDateString('vi-VN') : 'N/A' %></td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/admin/posts/edit/<%= post.id %>" class="btn-edit">✏️ Sửa</a>
                                                <form action="/admin/posts/delete/<%= post.id %>" method="POST" style="display: inline-block;">
                                                    <button type="submit" class="btn-delete" onclick="return confirm('Bạn có chắc chắn muốn xóa bài viết này không?');">🗑️ Xóa</button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="empty-state">
                            <h3>📝 Chưa có bản nháp nào</h3>
                            <p>Hãy tạo bản nháp đầu tiên!</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Tab Content - Archived Posts -->
            <div id="tab-archived" class="tab-content">
                <div class="table-container">
                    <% 
                    const archivedPosts = posts ? posts.filter(p => p.status === 'archived') : [];
                    %>
                    <% if (archivedPosts.length > 0) { %>
                        <table>
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Tiêu đề</th>
                                    <th>Danh mục</th>
                                    <th>Loại</th>
                                    <th>Ngày lưu trữ</th>
                                    <th>Hành động</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% archivedPosts.forEach(post => { %>
                                    <tr>
                                        <td><%= post.id %></td>
                                        <td><strong><%= post.title %></strong></td>
                                        <td><%= post.category ? post.category.name : 'N/A' %></td>
                                        <td><%= post.type || 'N/A' %></td>
                                        <td><%= post.updatedAt ? new Date(post.updatedAt).toLocaleDateString('vi-VN') : 'N/A' %></td>
                                        <td>
                                            <div class="action-buttons">
                                                <a href="/admin/posts/edit/<%= post.id %>" class="btn-edit">✏️ Sửa</a>
                        <form action="/admin/posts/delete/<%= post.id %>" method="POST" style="display: inline-block;">
                                                    <button type="submit" class="btn-delete" onclick="return confirm('Bạn có chắc chắn muốn xóa bài viết này không?');">🗑️ Xóa</button>
                        </form>
                                            </div>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
<% } else { %>
                        <div class="empty-state">
                            <h3>📦 Chưa có bài viết nào được lưu trữ</h3>
                            <p>Chưa có bài viết nào được đưa vào lưu trữ!</p>
                        </div>
<% } %>
                </div>
            </div>
        </div>
    </div>

    <script>
        function showTab(tabName) {
            // Hide all tab contents
            const tabContents = document.querySelectorAll('.tab-content');
            tabContents.forEach(content => {
                content.classList.remove('active');
            });
            
            // Remove active class from all tab buttons
            const tabButtons = document.querySelectorAll('.tab-button');
            tabButtons.forEach(button => {
                button.classList.remove('active');
            });
            
            // Show selected tab content
            document.getElementById('tab-' + tabName).classList.add('active');
            
            // Add active class to clicked button
            event.target.classList.add('active');
        }
        
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('open');
        }
        
        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const mobileToggle = document.querySelector('.mobile-menu-toggle');
            
            if (window.innerWidth <= 768) {
                if (!sidebar.contains(event.target) && !mobileToggle.contains(event.target)) {
                    sidebar.classList.remove('open');
                }
            }
        });
    </script>
</body>
</html>