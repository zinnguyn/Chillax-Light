<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>HILLAX LIGHT</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
        }

        /* General Body Styles (optional but recommended for consistency) */
        body {
            margin: 0;
            font-family: Arial, sans-serif; /* Hoặc font tương tự như trong mẫu */
        }

        /* Header Container - Bao bọc toàn bộ header để căn giữa */
        /* Thêm class này vào thẻ <header> của bạn trong HTML */
        header.main-header-wrapper { /* Tên class mới để bao bọc toàn bộ header */
            background-color: #fff; /* Nền trắng cho toàn bộ phần header */
            box-shadow: 0 2px 5px rgba(0,0,0,0.05); /* Có thể thêm bóng nhẹ nếu muốn */
        }


        /* Header Top */
        .top-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            border-bottom: 1px solid #eee;
            box-sizing: border-box;

            /* THÊM CÁI NÀY ĐỂ CĂN GIỮA NỘI DUNG TOP-HEADER */
            max-width: 1200px; /* Giới hạn chiều rộng tối đa */
            margin: 0 auto; /* Căn giữa theo chiều ngang */
        }

        .logo img {
            height: 50px;
        }

        .search-box {
            flex-grow: 1;
            margin: 0 30px;
            position: relative;
            max-width: 500px;
        }

        .search-box input {
            width: 100%;
            padding: 10px 40px 10px 20px;
            border: 1px solid #ff7dac;
            border-radius: 20px;
            outline: none;
            font-size: 14px;
            color: #333;
        }

        .search-box button {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: #ff7dac;
            font-size: 18px;
            cursor: pointer;
        }


        .actions {
            display: flex;
            align-items: center; /* Đảm bảo các nút được căn giữa theo chiều dọc trong container này */
            gap: 10px;
            flex-shrink: 0;
            /* margin-left: auto; -- Không cần nữa vì top-header đã dùng justify-content: space-between */
        }

       /* Trong thẻ <style> của bạn */

.actions a {
    background: #ff7dac;
    color: white;
    width: 160px; /* Đặt chiều rộng cố định */
    height: 45px; /* Đặt chiều cao cố định */
    padding: 0; /* Đảm bảo không có padding */

    border-radius: 25px;
    font-weight: bold;
    text-decoration: none;
    transition: background 0.2s ease-in-out;
    white-space: nowrap;

    /* Các thuộc tính Flexbox để căn chỉnh nội dung bên trong nút */
    display: flex;
    align-items: center; /* Căn giữa nội dung theo chiều dọc */
    justify-content: center; /* Căn giữa nội dung theo chiều ngang */
    gap: 5px; /* Khoảng cách giữa icon và text nếu có */

    box-sizing: border-box; /* Đảm bảo border và padding (nếu có) được tính vào width/height */
    /* Thêm các thuộc tính này để ép chiều cao nội dung và căn chỉnh */
    line-height: 1; /* Ép chiều cao dòng của văn bản trong nút */
    overflow: hidden; /* Đảm bảo không có nội dung tràn ra khỏi chiều cao cố định */
}

/* Tinh chỉnh riêng cho icon trong nút giỏ hàng */
.actions a .fas {
    font-size: 1em; /* Kích thước icon bằng với font-size của text bình thường */
    line-height: 1; /* Đảm bảo chiều cao dòng của icon */
    vertical-align: middle; /* Căn chỉnh icon theo chiều dọc với text */
    /* Điều chỉnh vị trí pixel chính xác hơn nếu cần */
    position: relative;
    top: -1px; /* Thử thay đổi giá trị này (-1px, 0px, 1px) để xem icon có thẳng hàng hơn không */
    /* Ví dụ: top: -1px; nếu icon vẫn hơi cao, hoặc top: 1px; nếu icon hơi thấp */
}

        /* Navigation Bar */
        .nav-bar {
            background-color: #ff7dac;
            padding: 12px 0; /* Giữ padding này */
        }

        .nav-list {
            list-style: none;
            display: flex;
            justify-content: center; /* Căn giữa các mục nav */
            margin: 0;
            padding: 0;
            gap: 30px;

            /* THÊM CÁI NÀY ĐỂ CĂN GIỮA NỘI DUNG NAV-BAR */
            max-width: 1200px; /* Giới hạn chiều rộng tối đa */
            margin: 0 auto; /* Căn giữa theo chiều ngang */
        }

        .nav-list li a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            padding: 8px 18px;
            border-radius: 25px;
            transition: background 0.2s ease-in-out, border 0.2s ease-in-out;
            white-space: nowrap;
        }

        .nav-list li a.active {
            border: 1px solid white;
            background-color: transparent;
        }

        .nav-list li a:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        /* Dropdown Menu (giữ nguyên, vì không thể thấy rõ trong ảnh) */
        .dropdown {
            position: relative;
        }

        .dropdown-menu {
            display: none;
            position: absolute;
            background-color: white;
            top: 100%;
            left: 0;
            border-radius: 8px;
            overflow: hidden;
            min-width: 180px;
            z-index: 999;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.1);
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .dropdown-menu li a {
            display: block;
            padding: 10px 15px;
            color: #333;
            text-decoration: none;
            border-bottom: 1px solid #eee;
            font-weight: normal;
        }

        .dropdown-menu li:last-child a {
            border-bottom: none;
        }

        .dropdown-menu li a:hover {
            background-color: #f0f0f0;
            color: #ff7dac;
        }

        .dropdown:hover .dropdown-menu {
            display: block;
        }

        /* CSS cho cart-counter */
        #cart-counter {
            background: #fff !important;
            color: #ff7dac !important;
            border-radius: 50% !important;
            width: 20px !important;
            height: 20px !important;
            display: inline-flex !important;
            align-items: center !important;
            justify-content: center !important;
            font-size: 12px !important;
            margin: 0 5px !important;
            font-weight: bold !important;
        }
    </style>
</head>
<body>
    <header class="main-header-wrapper">
        <div class="top-header">
            <div class="logo">
                <img src="/img/logo.png" alt="HILLAX LIGHT">
            </div>

            <div class="search-box">
                <form action="/search" method="GET">
                    <input type="text" name="q" placeholder="TÌM KIẾM........">
                    <button type="submit"><i class="fas fa-search"></i></button>
                </form>
            </div>

            <div class="actions">
              <!-- Popup cho đăng nhập/đăng ký -->
              <a href="#" class="btn btn-pink" data-bs-toggle="modal" data-bs-target="#authModal">
                  Đăng nhập / Đăng ký
              </a>
          
              <!-- Link giỏ hàng với counter -->
              <a href="/cart" class="btn btn-pink">
                GIỎ HÀNG 
                <span id="cart-counter" style="display: none;">0</span>
                <span id="cart-total">0₫</span> 
                <i class="fas fa-shopping-cart"></i>
            </a>
            
            <a href="/user" class="btn btn-outline">
              <i class="fas fa-user"></i> TÀI KHOẢN
            </a>
          </div>
        </div>

        <nav class="nav-bar">
            <ul class="nav-list">
                <li><a href="/" class="active">TRANG CHỦ</a></li>
                <li><a href="/about">GIỚI THIỆU</a></li>
                <li class="dropdown">
                    <a href="/products">CỬA HÀNG <i class="fas fa-caret-down"></i></a>
                    <ul class="dropdown-menu">
                        <li><a href="/products?category=Đèn Hiệu Ứng Bầu Trời">Đèn Hiệu Ứng Bầu Trời</a></li>
                        <li><a href="/products?category=Đèn Trang Trí">Đèn Trang Trí</a></li>
                        <li><a href="/products?category=Đèn Quà Tặng">Đèn Quà Tặng</a></li>
                        <li><a href="/products?category=Đèn phòng ngủ">Đèn Phòng Ngủ</a></li>
                    </ul>
                </li>
                <li><a href="/news">TIN TỨC</a></li>
                <li><a href="/contact">LIÊN HỆ</a></li>
            </ul>
        </nav>
    </header>
    <% if (error_msg && error_msg.length > 0) { %>
      <div class="alert alert-danger">
        <%= error_msg %>
      </div>
    <% } %>
    
    <script>
        // Lắng nghe sự kiện click trên các liên kết danh mục sản phẩm trong dropdown
        document.querySelectorAll('.nav-category-link').forEach(link => {
            link.addEventListener('click', function(event) {
                // Không ngăn chặn mặc định, để trình duyệt chuyển hướng đến /shop?category=...
                // Trang shop.ejs sẽ đọc URL và hiển thị đúng sản phẩm
            });
        });

        // Lắng nghe sự kiện click trên liên kết "CỬA HÀNG" chính trong navbar
        document.querySelector('.nav-shop-link').addEventListener('click', function(event) {
            // Không ngăn chặn mặc định, để trình duyệt chuyển hướng đến /shop
            // Trang shop.ejs sẽ tải tất cả sản phẩm (vì không có category)
        });
    </script>
    
    <!-- Load cart.js để xử lý giỏ hàng -->
    <script src="/js/cart.js"></script>
</body>
</html>